[ ]

### EX-01 — Counter + Actions (createSlice)

🎯 Objetivo

Entender state, actions, reducers e Provider

Crie um contador simples para aprender os conceitos fundamentais do Redux. Este é o primeiro passo para dominar o gerenciamento de estado global.

* Store com `configureStore` e um slice `counter`
* Actions: `increment`, `decrement`, `addByAmount(n)`
* UI com botões e input numérico
* Persistir valor em `localStorage` **EXTRA**
